complete all sensor code

impl fram wrapper (maybe in Rust)

bmi088 repalces bno055

impl startup health checkups

figure out platformIO tests, run tests before startup on debug

what happens to assert fail on runtime

change return to EXIT codes rather than bool

add a wiring diagram for the microcontroller